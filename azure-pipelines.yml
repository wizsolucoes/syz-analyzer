# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  batch: 'true'

resources:
  repositories:
    - repository: nodepackagetemplate
      type: git
      name: WizPipelines/node-package-template

extends:
  template: cicd.yml@nodepackagetemplate
  parameters:
    customEndpoint: 'npmjs'
